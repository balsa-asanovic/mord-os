import "./icon.css";
import Draggable from 'react-draggable';

const Icon = () => {

    return (
        <Draggable>
        <div className="icon-main">

        </div>
        </Draggable>
    )
};

export default Icon;